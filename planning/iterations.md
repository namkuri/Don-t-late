# iterations.md — 사람 반려 → 교정 반복 대장 (append-only)

- [M1] 비콘 v1 사람 반려(포커스 과민·파라미터 부재·상자 소멸) → v2 교정(IFocusGate·_padSize·드롭 물리)
- [M1] 별밭 발주의 에이전트 자기보고("가로등 공존")가 스크린샷과 불일치 → 관제 검수가 결함 적발:
  StreetLampLight가 Night 직행 점프(아침→밤 시각 조작) 시 미점등 (Evening 진입만 처리).
  관제 직접 수정 1줄 + 재검증(litSpots=9). 교훈: 자기보고보다 스크린샷 — 판정권한 분리가 실제로 일함.
- [M1] 간판 발광판(R11) 사람 반려: 발광판이 실제 GS25 프리팹 간판을 가림 — additive 판이 원본 간판 위를 덮는 구조 한계. 처방 연구 필요(간판 분리 익스포트 / 발광 데칼 SG 승격 / 블렌드 방식 변경) → 재발주 대기
- [M2] PR 검수 보고 오류(사람 적발): 관제가 "정상"으로 보고했으나 정확한 판정은 조건부 ACCEPT였음 — 품질 축만 검사, 절차 이탈 2건(무발주 자발+직교 추가 기록 누락)을 사람이 캐묻고서야 보고. D-038과 동일 병(검수 축 누락)의 재발 → PR 검수 체크리스트에 절차 정합 축 신설
- [M2] 관제가 원격 세션 대장의 행위 주체 표기("Director")를 무검증 신뢰 → 사람에게 2회 오설명 (사람 적발: "BGM 만든 적 없음") → PR 절차 검수에 '주체 표기 정확성' 항목 추가

- [M3] S-098 반려(2026-07-29 사람 적발): 버프 파란 fill이 HUD 배경 박스 밖 돌출 — 관제 캡처 검증이 '기능 존재'만 보고 레이아웃 결함을 반복 통과(폰 앱 센터 어긋남·겹침 동일 뿌리). 처방: 캡처 전용 검수 에이전트 신설(S-099) — 코드의 작성자≠판정자 분리를 픽셀에도 적용
- [M3] S-100 시작 버튼 불능(사람 적발): 원인 = **관제 사고** — S-098 게이지 호버 검증에서 가상 마우스 정리 시 `deviceId==2` 조건으로 **실물 마우스 디바이스를 제거**(가상은 잔존). 물리 클릭 이벤트 소멸 — 리스너·레이캐스트·플로우는 전 구간 정상이라 합성 클릭만 통과하는 기만적 상태. 진단 중 Invoke로 남규님 세션에 Home 전이 1회 주입(2차 침범). 처방: 에디터 재시작으로 native 재열거 + /deliver에 "가상 디바이스는 native==false 판별로만 제거·검증 후 디바이스 전수 확인" 박제
- [M3] S-104 엔딩 반려(2026-07-29 사람 적발 · S-105 교정): 관제가 발주서에 없는 방어 조건(completedCount>0)을 임의 추가 → 치트 정산 경로(배송 0건)에서 독백 차단. 빚은 세션 시작 시 양수 리셋이라 그 방어가 지키는 상황 자체가 불가능 — YAGNI 위반("일어날 수 없는 상황의 방어 코드 금지")이 실사용을 막은 전형. 교훈: 가드 한 줄도 발주서 밖이면 그 필요성을 스스로 증명하고 넣을 것
- [M3] S-112 텍스처 오판정(2026-07-30 사람 적발 · S-113 교정): 관제가 "임베디드 텍스처 0"으로 판정하고 Qwen png UV 우회 실험까지 했으나, 남규님이 인스펙터(Extract Textures 버튼·컬러 프리뷰)로 반증 — LoadAllAssetsAtPath는 미추출 임베디드 텍스처를 세지 못한다. 교훈: 에셋 내부 상태는 API 한 방이 아니라 임포터 세팅(Materials 탭)까지 봐야 한다. 교정: 자동 추출 규칙(2026-07-21 대기열) 집행으로 81장 회수
- [M3] S-116 reviewer REJECT 근거 기각(2026-07-30 관제 판단): 리뷰어가 District1SceneBuilder(에디터 빌더)의 GameObject.Find 5곳을 CODE_RULES §3.3 위반으로 반려. 기각 근거 — §3.3은 런타임 통신 규칙이고 에디터 빌더는 기존 코드 전반(DistrictSceneBuilder:45 등)이 Find를 표준 사용, 리뷰어의 대체 제안(FindAnyObjectByType)도 같은 검색 계열이라 지적이 형식적. deprecated 경고 2건(FindFirstObjectByType)만 유효해 FindAny로 교정. 그 외 9항목 전부 통과.
- [M3] S-116 District1 벚꽃 소멸 간헐(2026-07-30 관제 관측 · 미해결): 첫 데모 런에서 나무 2주(deco+TreeLine_02)의 모델 자식 GO가 파괴됨(래퍼만 잔존). 재현 시도 — 수동 변주 4종(Snow·Heat·Rain·밤낮)+5분 소크(자체 변주 다수)에서 재현 0. 파괴 주체 코드 수색(Destroy 전수 grep)에서 나무를 지울 경로 부재. 간헐 1회 사안으로 박제 — 촬영 전 리허설 1회 권장(재발 시 [교통사고] 로그 동반 여부 확인).
