# 라우팅 시트 RT-20260729-01 — socket-map District 현행화

> AAPP 정의서 §7.3 스키마 준수 — **첫 정식 발행 실증** (S-093 ②). 발주 봉투 축약형이 아닌
> 시트 원형으로 발행·실행·기록한다.

```yaml
routing:
  bom_id: scn_district_sockets      # 씬 데이터 자산 — 소켓 좌표 사본
  asset_type: scene_data
  template: STD-SCENE-DUMP          # scan → record → verify
  variations: [code-as-source]      # 씬 대신 빌더 코드가 정본 (D — 씬 미커밋 체제)
  steps: [scan, record, verify]
  target_socket: planning/socket-map.md "District.unity" 절
  est_total_min: 12
  status: done                      # 2026-07-29 실행 완료
```

## 실행 기록

| step | 내용 | 결과 |
|---|---|---|
| scan | DistrictSceneBuilder.cs·DistrictCargoSpawner.cs·CampStageBuilder.cs에서 소켓 상수 추출 (도로 x=0·슬롯 스킵·신호등·횡단보도·비콘 슬롯·트럭 DepartPoint) | ○ |
| record | socket-map.md District 절 교체 + Camp 신규 절 추가 | ○ |
| verify | 갱신 좌표를 빌더 소스와 grep 대조 (ROAD_X·BEACON_SLOTS_X·BuildTrafficLight 위치·DepartPoint 오프셋) | ○ |

- 리드: 12분(est) 대비 실측 ~8분.
