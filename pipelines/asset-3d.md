# 파이프라인: 3D 소품 (asset-3d)
담당: asset-generator (검증: reviewer) · 연결: CONNECTIONS #2 Meshy · #3 Trellis · #4 Nano Banana · #5 Blender

## 1. 기동 점검 (작업 전 매번)
- [ ] Meshy 또는 Trellis 응답 확인 — 둘 다 미연결 → **CONNECT_REQUEST(치명)**, 레인 정지
- [ ] Blender headless 확인 — 미연결 → CONNECT_REQUEST(높음), 폴백: 후처리를 사람 수동으로
- [ ] docs/STYLE.md·TECH_SPEC.md 존재 — 없으면 생성 거부(뼈대 미완성)
- 생성은 **Meshy/Trellis만** — 웹 임의 모델 다운로드 반입 금지(라이선스 리스크). 미연결 → CONNECT_REQUEST

## 2. 공정 단계
0. (계열 최초 1회) 컨셉 원화 1장 — Nano Banana. 이터레이션 금지, 방향 고정용
1. 생성 — 프롬프트 공식: [대상]+[STYLE 규격]+[스케일]+[원점]+[부정 제약]. 유사 3종 이상은 배치
2. 후처리 — bpy: 폴리 확인→초과 시 데시메이트, 원점 규약 교정, GLB 익스포트
   (생성물 착지는 `assets/_intake/meshy/` 등 도구명 폴더 — HARNESS §9, dest 직행 금지)
3. manifest 기록(=입장권: 프롬프트·라이선스·폴리·gen) → 검역 통과 후 dest 이동(파일명=bom_id) → _intake 원본 폐기
4. reviewer 검증 → 통과 시 unity-dev가 소켓 스왑

## 3. 자동교정 루프 (성공은 조용히, 실패만 시끄럽게)
| 게이트 실패 | 자가 교정 (cap 2) |
|---|---|
| 폴리 초과 | bpy decimate → 재측정. 그래도 초과 → 프롬프트에 폴리 명시 강화 후 1회 재생성 |
| 원점 불일치 | bpy 피봇 교정 → 재검 |
| 임포트 에러 | GLB 재익스포트(설정 점검) → 재임포트 |
| 팔레트/톤 이탈 | 텍스처 팔레트 양자화 시도 → 안 되면 프롬프트 부정 제약 추가 후 재생성 |
| 스케일 오류 | 스케일 시트 값으로 트랜스폼 교정 |
- 2회 실패 → 부모 보고(시도 내역 포함). 성공 시 반환은 `id·경로·폴리수` 1줄.

## 4. 실수 → 규칙 (append-only — 실수 낸 에이전트가 1줄 추가)
- [시드] 생성 프롬프트에 원점 규약 누락 → 모든 프롬프트에 [원점] 항 필수
- [시드] 유사 소품 개별 발사로 크레딧 낭비 → 3종 이상은 반드시 배치 생성
- [시드] 매니페스트 기록을 나중으로 미룸 → 저장과 기록은 한 동작 (미기록 = 미완료)
